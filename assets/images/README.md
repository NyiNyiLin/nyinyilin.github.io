# Image folders

Place future full-screen landing images in `hero/` and event-specific photos in the matching folder under `events/`.

Recommended hero filenames: `hero-01.webp` through `hero-05.webp`.

The five existing race photographs are stored in `highlights/` and displayed in the “Race day moments” section. The hero currently uses gradient placeholders until its separate background images are available.
